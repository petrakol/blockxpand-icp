type Holding = record {
  source: text;
  token: text;
  amount: text;
  status: text;
};

service: {
  "get_holdings": (principal) -> (vec Holding) query;
  "pools_graphql": (text) -> (text) query;
};
